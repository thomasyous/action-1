name: my first workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: checkout code
          uses: actions/checkout@v4

        - name: List and Read file
          run: |
            echo "Welcome to my first github action job"
            ls -ltra
            cat README.md

        - name: Generate ASCII Artwork
          run: cowsay -f dragon "run for cover, I am a DRAGON ....RAWR" >> dragin.txt


       
          